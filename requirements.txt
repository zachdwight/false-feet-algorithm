# Core dependencies
# (none required - uses only stdlib)

# Development dependencies
pytest>=6.0
pytest-cov>=2.10.0

# Optional visualization
matplotlib>=3.3.0

# Optional code quality
black>=21.0
mypy>=0.900
